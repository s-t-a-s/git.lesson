package lesson10;

public class ExplicitWait {
/*

    WebDriverWait wait = new WebDriverWait(driver, 10);
    wait.until(ExpectedConditions.presenceOfElementLocated(By.cssSelector(".someSelector")));

    ExpectedConditions - класс с набором методов “стандартных” ожиданий.



                WebDriver driver = new ChromeDriver();
                driver.get("http://kismia.com");
                WebElement myDynamicElement = (new WebDriverWait(driver, 10)).until(new ExpectedCondition<WebElement>(){

                    @Override
                    public WebElement apply(WebDriver d) {
                        return d.findElement(By.id("myDynamicElement"));
	             }});

*/

}
