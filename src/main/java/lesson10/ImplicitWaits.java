package lesson10;

public class ImplicitWaits {
    /*
    driver.manage().timeouts().implicitlyWait(10, TimeUnit.SECONDS); // Ожидание элементов
    driver.manage().timeouts().pageLoadTimeout(10, TimeUnit.SECONDS);  // Ожидание загрузки страницы
    driver.manage().timeouts().setScriptTimeout(10, TimeUnit.SECONDS); // Ожидание выполнения executeAsyncScript (JavascriptExecutor)
*/

}
