package lesson10;

public class JavascriptExecutor {

    /*

((JavascriptExecutor) driver).executeScript(String script, Object... args);
((JavascriptExecutor) driver).executeScript("arguments[0].scrollIntoView(true);", element); //Скролл к элементу
((JavascriptExecutor) driver).executeScript("return document.readyState"); // Состояние страницы (Загрузка)
((JavascriptExecutor) driver).executeScript( "window.open('')"); // Открыть новую вкладку в браузере
((JavascriptExecutor) driver).executeScript("arguments[0].remove();", element); // Удалить элемент
((JavascriptExecutor) driver).executeScript("arguments[0].style.border='3px solid "+color+"'", element); // "Подсветить" элемент

*/

}
